# D8 — Monitoring / Health / Regression
**Mission:** health endpoints, E2E, PDF byte-grep/readback, drift gates, source-of-truth checks. **Forbidden:** "production verified" without live proof.
**Impl:** /api/healthz + /api/central-brain/health + Playwright. **Status:** product E2E + drift gates to build.
